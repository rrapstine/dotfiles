return {
  roles = { 'lsp' },
  lsp = {
    -- Using nvim-lspconfig defaults:
    -- - filetypes: { 'bash', 'sh' }
    -- - root_markers: { '.git' }
    -- - settings: bashIde.globPattern customization already handled by defaults
  }
}