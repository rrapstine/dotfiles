return {
  roles = { 'lsp' },
  lsp = {
    -- Using nvim-lspconfig defaults:
    -- - filetypes: { 'dockerfile' }
    -- - root_markers: { 'Dockerfile' }
    -- - cmd: { 'docker-langserver', '--stdio' }
  }
}