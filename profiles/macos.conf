# macOS Profile Configuration  
# This profile now uses auto-discovery - no manual mappings needed!
# The system automatically discovers configs in:
#   - config/universal/ (always linked)
#   - config/macos/ (macOS-specific configs)

# Bin scripts (still need manual mapping)
bin/ec=$HOME/.local/bin/ec
bin/setup=$HOME/.local/bin/setup
bin/tp=$HOME/.local/bin/tp